services:
  - type: web
    name: CinemaAPI
    env: docker
    buildCommand: 'docker build -t cinemaapi .'
    startCommand: 'docker run -d -p 5000:80 cinemaapi'
    branch: master
    plan: free
    envVars:
      - key: ASPNETCORE_ENVIRONMENT
        value: Production
